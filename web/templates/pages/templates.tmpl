{{ define "content" }}
<section class="py-6">
  <h1 class="text-2xl font-bold">{{ tlang $.Lang "nav.templates" }}</h1>
  <p class="mt-2 text-gray-600">Coming soon.</p>
  <div class="mt-4 text-sm text-gray-500">Path: {{ $.Path }}</div>
</section>
{{ end }}

