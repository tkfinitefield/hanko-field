{
  "name": "hanko-web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "tailwind:build": "npx tailwindcss -c tailwind.config.js -i assets/css/input.css -o public/assets/app.css --minify",
    "tailwind:watch": "npx tailwindcss -c tailwind.config.js -i assets/css/input.css -o public/assets/app.css --watch",
    "assets:copy": "mkdir -p public/assets/js && cp node_modules/htmx.org/dist/htmx.min.js public/assets/js/htmx.min.js || true",
    "build": "npm run assets:copy && npm run tailwind:build",
    "dev:css": "npm run assets:copy && npm run tailwind:watch"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.47",
    "tailwindcss": "^3.4.14"
  },
  "dependencies": {
    "htmx.org": "^2.0.3"
  }
}

